# Server Configuration
PORT=4000

# Database
DB_TYPE=sqlite
SQLITE_FILE=data/waas.sqlite

# MySQL Configuration (only used if DB_TYPE=mysql)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=waas
DB_PORT=3306

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=

# Admin User Seed
ADMIN_EMAIL=admin@waas.local
ADMIN_PASSWORD=admin123

# Default Client User Seed
USER_EMAIL=user@waas.local
USER_PASSWORD=user123

# Security
JWT_SECRET=dev-secret
