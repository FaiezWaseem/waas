Waas frontend (Next.js) — TODO: scaffold with create-next-app. The frontend will provide:
- Signup/Login (NextAuth)
- Dashboard: sessions list, create session, show QR
- Chats view (live via websocket)
- Settings and billing

Start by running `npx create-next-app@latest client` inside waas/client
